import PackageStatistic from './PackageStatistic';

/** Represents packages statistics. */
export default interface PackagesStatistic {
	/** Individual packages statistics. */
	packages: PackageStatistic[];
}